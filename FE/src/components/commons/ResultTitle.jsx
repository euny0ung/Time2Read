const ResultTitle = ({ title }) => {
  return (
    <>
      <div className="flex items-center text-2xl font-bold text-center text-gray-700 font-inter">{title}</div>
    </>
  );
};

export default ResultTitle;
